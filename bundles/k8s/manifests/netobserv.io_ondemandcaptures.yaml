apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  creationTimestamp: null
  name: ondemandcaptures.netobserv.io
spec:
  group: netobserv.io
  names:
    kind: OnDemandCapture
    listKind: OnDemandCaptureList
    plural: ondemandcaptures
    shortNames:
    - odc
    - odcs
    singular: ondemandcapture
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.captureType
      name: Type
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.itemsCollected
      name: Items
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: OnDemandCapture is the Schema for the ondemandcaptures API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OnDemandCaptureSpec defines the desired state of OnDemandCapture
            properties:
              args:
                description: |-
                  args contains additional native CLI capture arguments, passed without a shell.
                  They follow structured settings and may override them. Connection, output,
                  and lifecycle options are managed by the operator and cannot be supplied.
                items:
                  type: string
                type: array
              captureType:
                description: 'captureType is the type of capture to perform: flows,
                  packets, or metrics'
                enum:
                - flows
                - packets
                - metrics
                type: string
              duration:
                description: duration is how long to capture (e.g. "5m", "1h30m")
                pattern: ^([0-9]+h)?([0-9]+m)?([0-9]+s)?$
                type: string
              filters:
                description: filters allows filtering the captured traffic
                properties:
                  action:
                    description: Accept or Reject action.
                    type: string
                  cidr:
                    description: IP CIDR filter.
                    type: string
                  direction:
                    description: Ingress or Egress direction.
                    type: string
                  dport:
                    description: Destination port.
                    format: int32
                    maximum: 65535
                    minimum: 0
                    type: integer
                  dport_range:
                    description: Destination port range in CLI syntax.
                    type: string
                  dports:
                    description: Comma-separated destination ports.
                    type: string
                  drops:
                    description: Capture only dropped traffic and enable packet-drop
                      tracking.
                    type: boolean
                  icmp_code:
                    description: ICMP code, including zero.
                    format: int32
                    maximum: 255
                    minimum: 0
                    type: integer
                  icmp_type:
                    description: ICMP type, including zero.
                    format: int32
                    maximum: 255
                    minimum: 0
                    type: integer
                  namespaces:
                    description: namespaces is a list of namespaces to capture traffic
                      from
                    items:
                      type: string
                    type: array
                  or:
                    description: or adds alternative filter groups, equivalent to
                      the CLI or separator.
                    items:
                      description: CaptureFilterRule uses the same filter names and
                        values as the native CLI.
                      properties:
                        action:
                          description: Accept or Reject action.
                          type: string
                        cidr:
                          description: IP CIDR filter.
                          type: string
                        direction:
                          description: Ingress or Egress direction.
                          type: string
                        dport:
                          description: Destination port.
                          format: int32
                          maximum: 65535
                          minimum: 0
                          type: integer
                        dport_range:
                          description: Destination port range in CLI syntax.
                          type: string
                        dports:
                          description: Comma-separated destination ports.
                          type: string
                        drops:
                          description: Capture only dropped traffic and enable packet-drop
                            tracking.
                          type: boolean
                        icmp_code:
                          description: ICMP code, including zero.
                          format: int32
                          maximum: 255
                          minimum: 0
                          type: integer
                        icmp_type:
                          description: ICMP type, including zero.
                          format: int32
                          maximum: 255
                          minimum: 0
                          type: integer
                        peer_cidr:
                          description: Peer CIDR filter.
                          type: string
                        peer_ip:
                          description: Peer IP address.
                          type: string
                        port:
                          description: Source or destination port.
                          format: int32
                          maximum: 65535
                          minimum: 0
                          type: integer
                        port_range:
                          description: Source or destination port range in CLI syntax.
                          type: string
                        ports:
                          description: Comma-separated source or destination ports.
                          type: string
                        protocol:
                          description: TCP, UDP, SCTP, ICMP, or ICMPv6.
                          type: string
                        sport:
                          description: Source port.
                          format: int32
                          maximum: 65535
                          minimum: 0
                          type: integer
                        sport_range:
                          description: Source port range in CLI syntax.
                          type: string
                        sports:
                          description: Comma-separated source ports.
                          type: string
                        tcp_flags:
                          description: TCP flags in CLI syntax.
                          type: string
                      type: object
                    type: array
                  peer_cidr:
                    description: Peer CIDR filter.
                    type: string
                  peer_ip:
                    description: Peer IP address.
                    type: string
                  podLabelSelector:
                    additionalProperties:
                      type: string
                    description: podLabelSelector filters pods by label selectors
                    type: object
                  port:
                    description: Source or destination port.
                    format: int32
                    maximum: 65535
                    minimum: 0
                    type: integer
                  port_range:
                    description: Source or destination port range in CLI syntax.
                    type: string
                  ports:
                    description: Comma-separated source or destination ports.
                    type: string
                  protocol:
                    description: TCP, UDP, SCTP, ICMP, or ICMPv6.
                    type: string
                  sport:
                    description: Source port.
                    format: int32
                    maximum: 65535
                    minimum: 0
                    type: integer
                  sport_range:
                    description: Source port range in CLI syntax.
                    type: string
                  sports:
                    description: Comma-separated source ports.
                    type: string
                  tcp_flags:
                    description: TCP flags in CLI syntax.
                    type: string
                type: object
              flowConfig:
                description: flowConfig is configuration specific to flow capture
                properties:
                  enableDNS:
                    description: enableDNS enables DNS latency tracking
                    type: boolean
                  enablePacketDrops:
                    description: enablePacketDrops enables packet drop tracking
                    type: boolean
                  enableRTT:
                    description: enableRTT enables TCP round-trip time tracking
                    type: boolean
                  sampling:
                    description: sampling is the sampling rate (e.g. 50 means 1 in
                      50 packets)
                    format: int32
                    minimum: 1
                    type: integer
                type: object
              interfaces:
                description: interfaces is a list of network interfaces to capture
                  from
                items:
                  type: string
                type: array
              maxBytes:
                description: maxBytes is the maximum number of bytes to capture
                format: int64
                minimum: 1
                type: integer
              metricsConfig:
                description: metricsConfig is configuration specific to metrics capture
                properties:
                  enableDNS:
                    description: enableDNS enables DNS metrics collection
                    type: boolean
                  enablePacketDrops:
                    description: enablePacketDrops enables packet drop metrics collection
                    type: boolean
                  enableRTT:
                    description: enableRTT enables latency metrics collection
                    type: boolean
                type: object
              nodeSelector:
                additionalProperties:
                  type: string
                description: |-
                  nodeSelector restricts which nodes the eBPF agents are deployed to.
                  When omitted, agents are deployed to all nodes.
                type: object
              packetConfig:
                description: packetConfig is configuration specific to packet capture
                properties:
                  port:
                    description: port filters packets by destination port
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                  protocol:
                    description: protocol filters packets by protocol (TCP, UDP, ICMP,
                      etc.)
                    type: string
                type: object
              resources:
                description: resources specifies the CPU and memory limits for the
                  capture pod
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              ttlSecondsAfterFinished:
                description: |-
                  ttlSecondsAfterFinished is the retention period after completion or failure.
                  Defaults to 86400 (24 hours) when omitted. Zero requests immediate cleanup.
                  Expiry deletes the CR and its resources, including undownloaded capture files.
                format: int32
                minimum: 0
                type: integer
            required:
            - captureType
            type: object
          status:
            description: OnDemandCaptureStatus defines the observed state of OnDemandCapture
            properties:
              bytesCollected:
                description: bytesCollected is the total number of bytes collected
                format: int64
                type: integer
              completionTime:
                description: completionTime is when the capture completed
                format: date-time
                type: string
              conditions:
                description: conditions represent the latest available observations
                  of the OnDemandCapture's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              daemonSetName:
                description: daemonSetName is the name of the temporary eBPF agent
                  DaemonSet
                type: string
              error:
                description: error is a message describing any error that occurred
                type: string
              fileServerPort:
                description: fileServerPort is the port where the file server is running
                format: int32
                maximum: 65535
                minimum: 1
                type: integer
              itemsCollected:
                description: itemsCollected is the number of items collected
                format: int64
                type: integer
              phase:
                description: phase represents the current phase of the capture
                enum:
                - Pending
                - Running
                - Completed
                - Failed
                type: string
              podIP:
                description: podIP is the IP address of the collector pod
                type: string
              podName:
                description: podName is the name of the capture pod
                type: string
              podNamespace:
                description: podNamespace is the namespace of the capture pod
                type: string
              startTime:
                description: startTime is when the capture started
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
